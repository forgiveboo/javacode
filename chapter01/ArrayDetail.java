public class ArrayDetail {
  public static void main(String[] args) {
    // 1. 数组中的元素只能是同一种数据类型或可以进行自动转换的类型
    // 2. 数组中的元素可以是任意类型 但是不能混用
    // 3. 数组创建后如果没有复制 则会有默认值 其中需要注意的是 boolean类型的默认值为false String类型的默认值为null
    // 4. 数组的下标是从0开始的
    // 5. 数组的下标必须在数组长度范围内 否则会保数组下标越界异常
    // 数组下标最大值为数组长度-1
    // 数组是引用类型 所以数组的的元素本质上是一个对象的引用
  }
}
